/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Linting is NOT skipped during builds. A build that hides its own lint
  // errors is not a passing build.
};
export default nextConfig;
